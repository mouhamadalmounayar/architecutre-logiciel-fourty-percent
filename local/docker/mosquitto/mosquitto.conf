# Enable persistence
persistence true
persistence_location /mosquitto/data/

# Define MQTT listening port
listener 1883

# Enable WebSocket (optional, for UI or tests)
listener 9001
protocol websockets

# Allow anonymous connections (for testing)
allow_anonymous true

# If you want to secure it, you can use passwd (see below)
# password_file /mosquitto/config/passwd
